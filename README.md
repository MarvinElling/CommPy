# CommPy

Lightweight Python utilities for communication engineering. See the documentation in the `docs/` folder and the full changelog in `CHANGELOG.md`.

Documentation
- Hosted documentation: `https://marvinelling.github.io/CommPy/` (or your GitHub Pages URL).
- Build locally:

```powershell
python -m pip install -r requirements-docs.txt
mkdocs serve
```

Changelog
- `CHANGELOG.md` — project changelog following Keep a Changelog format.
